import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary color: Primary buttons, icons, active states, main branding (#2446CE)
  static const Color primary = Color(0xFF2446CE);

  /// Primary Action/Wallet Gradient Start (#4F46E5)
  static const Color primaryGradientStart = Color(0xFF4F46E5);

  /// Primary Action/Wallet Gradient End (#9333EA)
  static const Color primaryGradientEnd = Color(0xFF9333EA);

  /// Positive amount/text color (#10B981)
  static const Color successGreen = Color(0xFF10B981);

  /// Negative amount/text color (#EF4444)
  static const Color errorRed = Color(0xFFEF4444);

  /// Out liner: Outlines, borders, secondary accents (#829BFF)
  static const Color outLiner = Color(0xFF829BFF);

  /// Black: Primary text / dark elements (#000000)
  static const Color black = Color(0xFF000000);

  /// White: Backgrounds, cards, button text (#FFFFFF)
  static const Color white = Color(0xFFFFFFFF);

  /// Light blue: Light backgrounds / selected states (#E1E7F6)
  static const Color lightBlue = Color(0xFFE1E7F6);

  /// Light gray: App background / subtle surfaces (#EBECF6)
  static const Color lightGray = Color(0xFFEBECF6);
  
  /// Lighter gray background for promo banner (#E5E7EB)
  static const Color promoGray = Color(0xFFF3F4F6);

  /// Gray font: Secondary text, hints, disabled text (#9B9B9B)
  static const Color grayFont = Color(0xFF9B9B9B);

  /// Icon Background Tints
  static const Color tintPurple = Color(0xFFF3E8FF);
  static const Color tintGreen = Color(0xFFD1FAE5);
  static const Color tintRed = Color(0xFFFEE2E2);
  static const Color tintBlue = Color(0xFFDBEAFE);
  
  /// Orange for Reward Coins
  static const Color orangeReward = Color(0xFFD97706);
}
